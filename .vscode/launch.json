{
    "version": "0.2.0",
    "configurations": [
        
       
        {
            "type": "chrome",
            "request": "launch",
            "file": "${workspaceFolder}/index.html",
           
            "webRoot": "${workspaceRoot}"
        }
    ]
}